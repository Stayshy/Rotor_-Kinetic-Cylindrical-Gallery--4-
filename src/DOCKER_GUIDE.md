# 🐳 Полное руководство по Docker

Это подробное руководство по запуску проекта "Альбом Воспоминаний" в Docker.

## 📋 Содержание

1. [Установка Docker](#установка-docker)
2. [Быстрый старт](#быстрый-старт)
3. [Режимы запуска](#режимы-запуска)
4. [Команды Docker](#команды-docker)
5. [Решение проблем](#решение-проблем)

## Установка Docker

### Windows

1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop)
2. Запустите установщик
3. Перезагрузите компьютер
4. Запустите Docker Desktop
5. Проверьте установку:
   ```cmd
   docker --version
   docker-compose --version
   ```

### macOS

1. Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop)
2. Перетащите Docker.app в папку Applications
3. Запустите Docker из Applications
4. Проверьте установку:
   ```bash
   docker --version
   docker-compose --version
   ```

### Linux (Ubuntu/Debian)

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo apt-get install docker-compose-plugin

# Перезагрузите систему
sudo reboot

# Проверка установки
docker --version
docker compose version
```

## Быстрый старт

### 🚀 Самый простой способ

#### Windows
Дважды кликните на файл `start.bat`

#### Linux/macOS
```bash
chmod +x start.sh
./start.sh
```

Приложение будет доступно по адресу: http://localhost:3000

## Режимы запуска

### 1. Production режим (рекомендуется)

**Особенности:**
- Оптимизированная сборка
- Минифицированный код
- Nginx веб-сервер
- Порт: 3000

**Запуск:**
```bash
# Linux/macOS
./start.sh

# Windows
start.bat

# Или вручную
docker-compose up --build -d
```

### 2. Development режим

**Особенности:**
- Hot Module Replacement (HMR)
- Подробные сообщения об ошибках
- Vite dev сервер
- Порт: 5173

**Запуск:**
```bash
# Linux/macOS
./start-dev.sh

# Windows
start-dev.bat

# Или вручную
docker-compose -f docker-compose.dev.yml up --build
```

## Команды Docker

### Основные команды

```bash
# Запустить в фоновом режиме
docker-compose up -d

# Запустить с пересборкой
docker-compose up --build

# Остановить контейнеры
docker-compose down

# Остановить и удалить volumes
docker-compose down -v

# Просмотр логов
docker-compose logs

# Следить за логами в реальном времени
docker-compose logs -f

# Перезапустить контейнеры
docker-compose restart
```

### Диагностика

```bash
# Список запущенных контейнеров
docker ps

# Список всех контейнеров (включая остановленные)
docker ps -a

# Использование ресурсов
docker stats

# Информация о контейнере
docker inspect album-vospominaniy

# Войти в контейнер
docker exec -it album-vospominaniy sh
```

### Очистка

```bash
# Удалить неиспользуемые образы
docker image prune

# Удалить всё неиспользуемое
docker system prune

# Агрессивная очистка (включая образы)
docker system prune -a

# Удалить конкретный контейнер
docker rm album-vospominaniy

# Удалить конкретный образ
docker rmi album-vospominaniy
```

## Решение проблем

### ❌ Порт уже используется

**Проблема:** `Error: bind: address already in use`

**Решение:**

1. **Найти процесс, использующий порт:**
   ```bash
   # Linux/macOS
   lsof -i :3000
   
   # Windows
   netstat -ano | findstr :3000
   ```

2. **Изменить порт в docker-compose.yml:**
   ```yaml
   ports:
     - "8080:80"  # Используйте другой порт
   ```

### ❌ Docker не запускается

**Windows:**
- Убедитесь, что виртуализация включена в BIOS
- Проверьте, что WSL 2 установлен
- Перезапустите Docker Desktop

**Linux:**
- Проверьте, что Docker daemon запущен:
  ```bash
  sudo systemctl status docker
  sudo systemctl start docker
  ```

### ❌ Ошибка при сборке

**Решение:**
```bash
# Очистить кэш и пересобрать
docker-compose build --no-cache
docker system prune -a
docker-compose up --build
```

### ❌ Нет доступа к интернету внутри контейнера

**Решение:**
```bash
# Перезапустить Docker
sudo systemctl restart docker

# Или в Docker Desktop: Settings → Reset → Restart
```

### ❌ Контейнер постоянно перезапускается

**Диагностика:**
```bash
# Посмотреть логи
docker-compose logs -f

# Проверить статус
docker ps -a
```

### ❌ Недостаточно места на диске

**Решение:**
```bash
# Удалить неиспользуемые данные
docker system prune -a --volumes

# Проверить использование места
docker system df
```

## Продвинутые настройки

### Изменение ресурсов контейнера

Отредактируйте `docker-compose.yml`:

```yaml
services:
  app:
    # ... другие настройки
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### Использование переменных окружения

Создайте файл `.env`:

```env
PORT=3000
NODE_ENV=production
```

Используйте в `docker-compose.yml`:

```yaml
services:
  app:
    ports:
      - "${PORT}:80"
    environment:
      - NODE_ENV=${NODE_ENV}
```

### Подключение к внешним сервисам

```yaml
services:
  app:
    # ... другие настройки
    depends_on:
      - database
    networks:
      - app-network
      - external-network

  database:
    image: postgres:latest
    # ... настройки базы данных

networks:
  app-network:
    driver: bridge
  external-network:
    external: true
```

## Мониторинг и логирование

### Просмотр логов с фильтрацией

```bash
# Только ошибки
docker-compose logs | grep -i error

# С временными метками
docker-compose logs -t

# Последние N строк
docker-compose logs --tail=100
```

### Health check

Проверьте статус приложения:

```bash
curl http://localhost:3000/health
```

## Docker vs Локальная разработка

| Аспект | Docker | Локальная разработка |
|--------|--------|---------------------|
| Настройка | Одна команда | npm install |
| Изоляция | Полная | Нет |
| Производительность | Может быть медленнее | Быстрее |
| Воспроизводимость | Гарантирована | Зависит от окружения |
| Развертывание | Легкое | Сложнее |

## Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Docker Hub](https://hub.docker.com/)
- [Best practices](https://docs.docker.com/develop/dev-best-practices/)

## Чек-лист перед развертыванием

- [ ] Docker и Docker Compose установлены
- [ ] Порты 3000/5173 свободны
- [ ] Достаточно места на диске (минимум 2GB)
- [ ] Интернет-соединение стабильно
- [ ] Docker Desktop запущен (Windows/macOS)
- [ ] Пользователь добавлен в группу docker (Linux)

---

**Нужна помощь?** Создайте issue в репозитории проекта.
