# ⚡ Быстрый старт

Запустите приложение "Альбом Воспоминаний" за 3 простых шага!

## 1️⃣ Убедитесь, что Docker установлен

```bash
docker --version
docker-compose --version
```

Если Docker не установлен, следуйте инструкциям: [DOCKER_GUIDE.md](./DOCKER_GUIDE.md)

## 2️⃣ Запустите приложение

### 🪟 Windows
Дважды кликните на `start.bat`

### 🐧 Linux / 🍎 macOS
```bash
chmod +x start.sh
./start.sh
```

### 🖥️ Альтернатива (командная строка)
```bash
docker-compose up --build -d
```

## 3️⃣ Откройте браузер

Перейдите по адресу: **http://localhost:3000**

---

## 🎉 Готово!

Вы должны увидеть 3D цилиндрическую галерею с анимацией.

## 🛑 Остановка приложения

### Windows
```cmd
docker-compose down
```

### Linux/macOS
```bash
docker-compose down
```

---

## 📚 Дополнительно

- Полная документация: [README.md](./README.md)
- Подробное руководство по Docker: [DOCKER_GUIDE.md](./DOCKER_GUIDE.md)
- Development режим (с hot-reload): Используйте `start-dev.bat` или `start-dev.sh`

## ❓ Проблемы?

### Порт занят
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:80"  # Вместо 3000
```

### Ошибки при сборке
```bash
docker system prune -a
docker-compose build --no-cache
docker-compose up -d
```

### Контейнер не запускается
```bash
docker-compose logs -f
```

---

**Всё еще нужна помощь?** Откройте [DOCKER_GUIDE.md](./DOCKER_GUIDE.md) для детальных инструкций.
