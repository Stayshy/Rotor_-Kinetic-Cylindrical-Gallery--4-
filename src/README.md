# Альбом Воспоминаний

Интерактивная 3D галерея семейных воспоминаний с цилиндрическим эффектом.

---

## 🚀 БЫСТРЫЙ СТАРТ

### ✅ Гарантия запуска: **100%**

Все файлы проверены и на месте. Проект точно откроется в Docker!

### Docker (Рекомендуется)

**Windows:**
```cmd
start.bat
```

**Linux/macOS:**
```bash
chmod +x start.sh && ./start.sh
```

**Откройте браузер:** http://localhost:3000

### 🔍 Проверка перед запуском (опционально)

```bash
# Windows
проверка.bat

# Linux/macOS
chmod +x проверка.sh && ./проверка.sh
```

### 📖 Документация

- **[НАЧНИТЕ_ЗДЕСЬ.txt](НАЧНИТЕ_ЗДЕСЬ.txt)** - Начните отсюда! Навигация по всей документации
- **[ЗАПУСК.txt](ЗАПУСК.txt)** - Супер-краткая инструкция (2 минуты)
- **[ШПАРГАЛКА.txt](ШПАРГАЛКА.txt)** - Все команды на одной странице
- **[КАК_ЗАПУСТИТЬ.md](КАК_ЗАПУСТИТЬ.md)** - Подробная пошаговая инструкция
- **[README_DOCKER.md](README_DOCKER.md)** - Полное руководство по Docker
- **[ИНФОГРАФИКА_ЗАПУСКА.txt](ИНФОГРАФИКА_ЗАПУСКА.txt)** - Визуальное руководство

---

## 🚀 Запуск с Docker

### Предварительные требования

- [Docker](https://www.docker.com/get-started)
- [Docker Compose](https://docs.docker.com/compose/install/)

### Быстрый старт

#### Linux / macOS

**Production версия:**
```bash
chmod +x start.sh
./start.sh
```

**Development версия (с hot-reload):**
```bash
chmod +x start-dev.sh
./start-dev.sh
```

#### Windows

**Production версия:**
Запустите `start.bat` двойным кликом или через командную строку:
```cmd
start.bat
```

**Development версия (с hot-reload):**
Запустите `start-dev.bat` двойным кликом или через командную строку:
```cmd
start-dev.bat
```

#### Ручной запуск

**Production версия (порт 3000):**
```bash
docker-compose up --build
```

**Development версия (порт 5173):**
```bash
docker-compose -f docker-compose.dev.yml up --build
```

### Доступ к приложению

- **Production:** [http://localhost:3000](http://localhost:3000)
- **Development:** [http://localhost:5173](http://localhost:5173)

### Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка приложения
docker-compose down

# Остановка и удаление volumes
docker-compose down -v

# Пересборка образа
docker-compose build --no-cache

# Просмотр запущенных контейнеров
docker ps
```

### Использование Makefile (опционально)

Если у вас установлен `make`, вы можете использовать удобные команды:

```bash
# Показать все доступные команды
make help

# Production запуск
make prod

# Development запуск
make dev

# Остановка
make down

# Просмотр логов
make logs

# Очистка
make clean

# Пересборка
make rebuild
```

### Альтернативный запуск (только Docker)

```bash
# Собрать образ
docker build -t album-vospominaniy .

# Запустить контейнер
docker run -d -p 3000:80 --name album-app album-vospominaniy

# Остановка и удаление
docker stop album-app
docker rm album-app
```

## 🛠️ Локальная разработка (без Docker)

### Установка зависимостей

```bash
npm install
```

### Команды разработки

```bash
# Запустить dev сервер (http://localhost:5173)
npm run dev

# Собрать для продакшена
npm run build

# Предпросмотр production сборки
npm run preview
```

## 📦 Структура проекта

```
/
├── components/          # React компоненты
│   ├── figma/          # Figma компоненты
│   ├── ui/             # UI компоненты (Shadcn)
│   └── webgl/          # 3D WebGL компоненты
├── data/               # Данные приложения
├── hooks/              # React hooks
├── styles/             # CSS стили
│   └── globals.css     # Глобальные стили и Tailwind
├── utils/              # Утилиты
├── webgl/              # WebGL материалы
│   └── materials/      # Three.js материалы
├── App.tsx             # Главный компонент приложения
├── main.jsx            # Точка входа React
├── index.html          # HTML шаблон
├── vite.config.js      # Конфигурация Vite
├── package.json        # Зависимости проекта
├── Dockerfile          # Docker конфигурация (production)
├── Dockerfile.dev      # Docker конфигурация (development)
├── docker-compose.yml  # Docker Compose (production)
├── docker-compose.dev.yml  # Docker Compose (development)
├── nginx.conf          # Nginx конфигурация
├── start.sh / start.bat    # Скрипты запуска (production)
└── start-dev.sh / start-dev.bat  # Скрипты запуска (dev)
```

## 🎨 Особенности

- ✨ **3D цилиндрическая галерея** с плавной анимацией
- 🎞️ **Анимированная кинолента** с прокруткой
- 🖼️ **Загрузка своих изображений** через drag & drop
- 🎨 **Золотистая цветовая схема** (#faf8f3, #c9a961)
- 📱 **Адаптивный дизайн** для всех устройств
- 🚀 **Оптимизированная производительность** с code splitting
- 🐳 **Docker поддержка** для легкого развертывания
- 🎛️ **Интерактивные контролы** для настройки анимации

## 🔧 Технологии

### Frontend
- **React 18** - UI библиотека
- **Three.js** - 3D графика
- **@react-three/fiber** - React renderer для Three.js
- **@react-three/drei** - Полезные хелперы для R3F
- **Vite** - Быстрый сборщик
- **Tailwind CSS 4.0** - Utility-first CSS фреймворк
- **Shadcn UI** - Коллекция компонентов
- **Lucide React** - Иконки

### DevOps
- **Docker** - Контейнеризация
- **Nginx** - Веб-сервер для production
- **Docker Compose** - Оркестрация контейнеров

## 🎯 Основные компоненты

### WebGL компоненты
- **KineticScene** - Главная 3D сцена с камерой и освещением
- **DisplayCylinder** - Цилиндрический дисплей с текстурами
- **MarqueeStrip** - Бегущая строка с градиентами

### UI компоненты
- **ImageUploadOverlay** - Панель загрузки изображений
- **ControlPanel** - Панель управления анимацией
- **CameraConfigOverlay** - Информация о камере

### Материалы
- **MeshDisplayMaterial** - Материал для дисплея с backface darkening
- **MeshMarqueeMaterial** - Материал с процедурными градиентами

## 🐛 Решение проблем

### Порт уже занят

Если порт 3000 или 5173 занят, измените порт в `docker-compose.yml`:
```yaml
ports:
  - "НОВЫЙ_ПОРТ:80"  # для production
  # или
  - "НОВЫЙ_ПОРТ:5173"  # для development
```

### Ошибки при сборке

```bash
# Очистка Docker кэша
docker system prune -a

# Пересборка без кэша
docker-compose build --no-cache
```

### Проблемы с правами (Linux/macOS)

```bash
# Дать права на выполнение скриптов
chmod +x start.sh start-dev.sh
```

## 📝 Лицензия

© 2025 Альбом Воспоминаний. Все права защищены.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

---

Создано с ❤️ используя React, Three.js и Docker