# 🚀 Как запустить проект "Альбом Воспоминаний" в Docker

## 📋 Предварительные требования

Перед началом убедитесь, что у вас установлено:

- **Docker Desktop** (для Windows/Mac) или **Docker Engine** (для Linux)
- **Docker Compose** (обычно входит в Docker Desktop)

### Проверка установки

Откройте терминал/командную строку и выполните:

```bash
docker --version
docker-compose --version
```

Если команды выполнились без ошибок и показали версии - всё готово! Если нет, установите Docker:

- **Windows/Mac**: https://www.docker.com/products/docker-desktop
- **Linux**: https://docs.docker.com/engine/install/

---

## 🎯 БЫСТРЫЙ СТАРТ (3 способа)

### Способ 1: Автоматические скрипты (САМЫЙ ПРОСТОЙ) ⭐

#### Windows:
1. Найдите файл `start.bat` в папке проекта
2. Дважды кликните по нему
3. Дождитесь окончания установки
4. Откройте браузер: http://localhost:3000

#### Linux / macOS:
```bash
chmod +x start.sh
./start.sh
```
Откройте браузер: http://localhost:3000

---

### Способ 2: Docker Compose (вручную)

```bash
# Сборка и запуск в одной команде
docker-compose up --build -d

# Приложение будет доступно на http://localhost:3000
```

**Что происходит:**
- `-d` запускает контейнер в фоновом режиме
- `--build` собирает образ перед запуском

---

### Способ 3: Только Docker (без Compose)

```bash
# Шаг 1: Собрать образ
docker build -t album-vospominaniy .

# Шаг 2: Запустить контейнер
docker run -d -p 3000:80 --name album-app album-vospominaniy

# Приложение будет доступно на http://localhost:3000
```

---

## 📝 Пошаговая инструкция (для новичков)

### Шаг 1: Открыть терминал/командную строку

- **Windows**: Нажмите `Win + R`, введите `cmd`, нажмите Enter
- **Mac**: Откройте Terminal (Finder → Программы → Утилиты → Terminal)
- **Linux**: Ctrl + Alt + T

### Шаг 2: Перейти в папку проекта

```bash
cd путь/к/папке/проекта
```

Например:
```bash
# Windows
cd C:\Users\YourName\album-vospominaniy

# Mac/Linux
cd ~/Downloads/album-vospominaniy
```

### Шаг 3: Проверить, что Docker запущен

```bash
docker ps
```

Если команда выполнилась без ошибок - Docker запущен.

**Если ошибка:**
- **Windows/Mac**: Запустите Docker Desktop из меню Пуск
- **Linux**: `sudo systemctl start docker`

### Шаг 4: Запустить проект

```bash
docker-compose up --build -d
```

**Что вы увидите:**
```
[+] Building 120.5s
[+] Running 2/2
 ✔ Network album-vospominaniy_app-network  Created
 ✔ Container album-vospominaniy            Started
```

### Шаг 5: Открыть браузер

Перейдите по адресу: **http://localhost:3000**

---

## 🔍 Проверка статуса

### Посмотреть запущенные контейнеры:
```bash
docker ps
```

Вы должны увидеть:
```
CONTAINER ID   IMAGE              STATUS          PORTS
abc123def456   album-vospominaniy Up 2 minutes   0.0.0.0:3000->80/tcp
```

### Посмотреть логи:
```bash
docker-compose logs -f
```

Нажмите `Ctrl + C` чтобы выйти из просмотра логов.

### Проверить health check:
```bash
curl http://localhost:3000/health
```

Ответ должен быть: `healthy`

---

## 🛑 Остановка приложения

### Остановить и удалить контейнеры:
```bash
docker-compose down
```

### Только остановить (без удаления):
```bash
docker-compose stop
```

### Снова запустить остановленные контейнеры:
```bash
docker-compose start
```

---

## 🔧 Полезные команды

### Перезапустить приложение:
```bash
docker-compose restart
```

### Пересобрать без кэша (при изменении кода):
```bash
docker-compose build --no-cache
docker-compose up -d
```

### Войти внутрь контейнера:
```bash
docker exec -it album-vospominaniy sh
```

### Удалить всё (контейнеры, образы, volumes):
```bash
docker-compose down -v
docker rmi album-vospominaniy
```

---

## ❌ Решение проблем

### Проблема: Порт 3000 уже занят

**Симптом:**
```
Error: bind: address already in use
```

**Решение 1**: Найти и остановить процесс на порту 3000

```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F

# Linux/Mac
lsof -i :3000
kill -9 <PID>
```

**Решение 2**: Изменить порт в `docker-compose.yml`

Откройте файл и измените:
```yaml
ports:
  - "8080:80"  # Вместо 3000:80
```

Теперь приложение будет на http://localhost:8080

---

### Проблема: Docker daemon не запущен

**Симптом:**
```
Cannot connect to the Docker daemon
```

**Решение:**
- **Windows/Mac**: Запустите Docker Desktop
- **Linux**: 
  ```bash
  sudo systemctl start docker
  sudo systemctl enable docker
  ```

---

### Проблема: Ошибка при сборке

**Симптом:**
```
ERROR: failed to solve
```

**Решение:**
```bash
# Очистить Docker кэш
docker system prune -a

# Пересобрать без кэша
docker-compose build --no-cache

# Запустить заново
docker-compose up -d
```

---

### Проблема: Страница не загружается

**Шаг 1**: Проверьте, что контейнер запущен
```bash
docker ps
```

**Шаг 2**: Посмотрите логи
```bash
docker-compose logs -f
```

**Шаг 3**: Проверьте health check
```bash
curl http://localhost:3000/health
```

**Шаг 4**: Попробуйте пересобрать
```bash
docker-compose down
docker-compose up --build -d
```

---

### Проблема: Не хватает места на диске

**Симптом:**
```
no space left on device
```

**Решение:**
```bash
# Удалить неиспользуемые ресурсы
docker system prune -a --volumes

# Посмотреть использование
docker system df
```

---

## 🎨 Режимы работы

### Production режим (по умолчанию)
- Оптимизированная сборка
- Nginx веб-сервер
- Порт: 3000
- Запуск: `docker-compose up -d` или `start.bat`/`start.sh`

### Development режим (с горячей перезагрузкой)
- Vite dev server
- Hot Module Replacement
- Порт: 5173
- Запуск: `docker-compose -f docker-compose.dev.yml up` или `start-dev.bat`/`start-dev.sh`

---

## 📦 Что внутри контейнера?

```
Dockerfile:
├── Stage 1: Builder
│   ├── Node.js 20 Alpine
│   ├── npm install
│   └── npm run build
│
└── Stage 2: Production
    ├── Nginx Alpine
    ├── Готовые файлы из Stage 1
    └── Nginx конфигурация
```

---

## 🌐 Адреса доступа

- **Приложение**: http://localhost:3000
- **Health check**: http://localhost:3000/health
- **Dev режим**: http://localhost:5173 (если используете docker-compose.dev.yml)

---

## 📚 Дополнительные ресурсы

- [Полная документация](README.md)
- [Руководство по Docker](DOCKER_GUIDE.md)
- [Быстрый старт](QUICKSTART.md)

---

## ✅ Чек-лист готовности

Перед запуском убедитесь:

- [ ] Docker установлен и запущен
- [ ] Docker Compose установлен
- [ ] Порт 3000 свободен
- [ ] Минимум 2GB свободного места на диске
- [ ] Интернет-соединение активно (для загрузки образов)
- [ ] Вы находитесь в папке проекта (там где Dockerfile)

---

## 🎉 Готово!

После успешного запуска вы увидите:
- 3D цилиндрическую галерею с вращением
- Анимированную киноленту
- Золотистую цветовую схему
- Интерактивные контролы

**Enjoy!** 🚀

---

## 💬 Нужна помощь?

Если что-то не работает:
1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи: `docker-compose logs -f`
3. Откройте issue в репозитории проекта

---

© 2025 Альбом Воспоминаний
